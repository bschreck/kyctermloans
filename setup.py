from setuptools import setup

# Note: calling the package nostra-core so when we have a python client no pkg name collision
setup(name="kyctermloans", packages=["kyctermloans"], include_package_data=True)
